name: test
on:
  workflow_dispatch:

jobs:
  test:
    name: check license
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
    - uses: actions/checkout@v3
    - uses: ./
      with:
        licenseWhitelist: 'MIT'
